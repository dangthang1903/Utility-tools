{
  "name": "utility-tools-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Utility Tools Hub: Vite React Frontend & NestJS Backend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "build-all": "npm run build --prefix frontend && npm run build --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
